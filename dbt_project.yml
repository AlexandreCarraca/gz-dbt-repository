name: 'my_new_project'
version: '1.0.0'
config-version: 2
profile: 'default'

models:
  my_new_project:
      staging:
        +materialized: view
      intermediate:
        +materialized: view
      mart:
        +materialized: table
